{
  "name": "microsaas-theme",
  "version": "1.0.0",
  "description": "Tema de Blocos WordPress Full Site Editing (FSE) para Micro-SaaS e Startups",
  "author": "Antigravity",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "build": "wp-scripts build --webpack-src-dir=src/blocks --output-path=build/blocks",
    "start": "wp-scripts start --webpack-src-dir=src/blocks --output-path=build/blocks",
    "lint:css": "wp-scripts lint-style",
    "lint:js": "wp-scripts lint-js"
  },
  "devDependencies": {
    "@wordpress/scripts": "^30.0.0"
  }
}
