"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// packages/ui/src/calendar/calendar.tsx
var calendar_exports = {};
__export(calendar_exports, {
  Calendar: () => Calendar
});
module.exports = __toCommonJS(calendar_exports);
var import_react = require("@daypicker/react");
var import_locale = require("@daypicker/react/locale");
var import_element = require("@wordpress/element");
var import_constants = require("./utils/constants.cjs");
var import_misc = require("./utils/misc.cjs");
var import_use_controlled_value = require("./utils/use-controlled-value.cjs");
var import_use_localization_props = require("./utils/use-localization-props.cjs");
var import_root_context = require("./utils/root-context.cjs");
var import_jsx_runtime = require("react/jsx-runtime");
var Calendar = (0, import_element.forwardRef)(
  function Calendar2({
    defaultValue,
    value: valueProp,
    onValueChange,
    numberOfMonths = 1,
    locale = import_locale.enUS,
    timeZone,
    render,
    labels: customLabels,
    ...props
  }, ref) {
    const localizationProps = (0, import_use_localization_props.useLocalizationProps)({
      locale,
      timeZone,
      mode: "single"
    });
    const labels = (0, import_element.useMemo)(
      () => customLabels ? { ...localizationProps.labels, ...customLabels } : localizationProps.labels,
      [localizationProps.labels, customLabels]
    );
    const onChange = (0, import_element.useCallback)(
      (selected2, triggerDate, modifiers, e) => {
        onValueChange?.(
          selected2 ?? null,
          triggerDate,
          modifiers,
          e
        );
      },
      [onValueChange]
    );
    const [selected, setSelected] = (0, import_use_controlled_value.useControlledValue)({
      defaultValue,
      value: valueProp,
      onChange
    });
    const rootContextValue = (0, import_element.useMemo)(
      () => ({ render, ref }),
      [render, ref]
    );
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_root_context.RootContext.Provider, { value: rootContextValue, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
      import_react.DayPicker,
      {
        ...import_constants.COMMON_PROPS,
        ...localizationProps,
        ...props,
        role: "application",
        mode: "single",
        numberOfMonths: (0, import_misc.clampNumberOfMonths)(numberOfMonths),
        labels,
        selected: selected ?? void 0,
        onSelect: setSelected
      }
    ) });
  }
);
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  Calendar
});
//# sourceMappingURL=calendar.cjs.map
