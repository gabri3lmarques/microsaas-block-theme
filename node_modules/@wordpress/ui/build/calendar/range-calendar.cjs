"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// packages/ui/src/calendar/range-calendar.tsx
var range_calendar_exports = {};
__export(range_calendar_exports, {
  RangeCalendar: () => RangeCalendar,
  usePreviewRange: () => usePreviewRange
});
module.exports = __toCommonJS(range_calendar_exports);
var import_date_fns = require("date-fns");
var import_react = require("@daypicker/react");
var import_locale = require("@daypicker/react/locale");
var import_element = require("@wordpress/element");
var import_constants = require("./utils/constants.cjs");
var import_misc = require("./utils/misc.cjs");
var import_use_controlled_value = require("./utils/use-controlled-value.cjs");
var import_use_localization_props = require("./utils/use-localization-props.cjs");
var import_root_context = require("./utils/root-context.cjs");
var import_jsx_runtime = require("react/jsx-runtime");
function usePreviewRange({
  value,
  hoveredDate,
  excludeDisabled,
  min,
  max,
  disabled
}) {
  return (0, import_element.useMemo)(() => {
    if (!hoveredDate || !value?.from) {
      return;
    }
    let previewHighlight;
    let potentialNewRange;
    if (hoveredDate < value.from) {
      previewHighlight = {
        from: hoveredDate,
        to: value.from
      };
      potentialNewRange = {
        from: hoveredDate,
        to: value.to ?? value.from
      };
    } else if (value.to && hoveredDate > value.from && hoveredDate < value.to) {
      previewHighlight = {
        from: value.from,
        to: hoveredDate
      };
      potentialNewRange = {
        from: value.from,
        to: hoveredDate
      };
    } else if (hoveredDate > value.from) {
      previewHighlight = {
        from: value.to ?? value.from,
        to: hoveredDate
      };
      potentialNewRange = {
        from: value.from,
        to: hoveredDate
      };
    }
    if (min !== void 0 && min > 0 && potentialNewRange && (0, import_date_fns.differenceInCalendarDays)(
      potentialNewRange.to,
      potentialNewRange.from
    ) < min) {
      previewHighlight = {
        from: hoveredDate,
        to: hoveredDate
      };
    }
    if (max !== void 0 && max > 0 && potentialNewRange && (0, import_date_fns.differenceInCalendarDays)(
      potentialNewRange.to,
      potentialNewRange.from
    ) > max) {
      previewHighlight = {
        from: hoveredDate,
        to: hoveredDate
      };
    }
    if (excludeDisabled && disabled && potentialNewRange && (0, import_react.rangeContainsModifiers)(potentialNewRange, disabled)) {
      previewHighlight = {
        from: hoveredDate,
        to: hoveredDate
      };
    }
    return previewHighlight;
  }, [value, hoveredDate, excludeDisabled, min, max, disabled]);
}
var RangeCalendar = (0, import_element.forwardRef)(
  function RangeCalendar2({
    defaultValue,
    value: valueProp,
    onValueChange,
    numberOfMonths = 1,
    excludeDisabled,
    min,
    max,
    disabled,
    locale = import_locale.enUS,
    timeZone,
    render,
    labels: customLabels,
    ...props
  }, ref) {
    const localizationProps = (0, import_use_localization_props.useLocalizationProps)({
      locale,
      timeZone,
      mode: "range"
    });
    const labels = (0, import_element.useMemo)(
      () => customLabels ? { ...localizationProps.labels, ...customLabels } : localizationProps.labels,
      [localizationProps.labels, customLabels]
    );
    const onChange = (0, import_element.useCallback)(
      (selected2, triggerDate, modifiers2, e) => {
        onValueChange?.(
          selected2 ?? null,
          triggerDate,
          modifiers2,
          e
        );
      },
      [onValueChange]
    );
    const [selected, setSelected] = (0, import_use_controlled_value.useControlledValue)({
      defaultValue,
      value: valueProp,
      onChange
    });
    const [hoveredDate, setHoveredDate] = (0, import_element.useState)(
      void 0
    );
    const previewRange = usePreviewRange({
      value: selected,
      hoveredDate,
      excludeDisabled,
      min,
      max,
      disabled
    });
    const modifiers = (0, import_element.useMemo)(() => {
      return {
        preview: previewRange,
        preview_start: previewRange?.from,
        preview_end: previewRange?.to
      };
    }, [previewRange]);
    const rootContextValue = (0, import_element.useMemo)(
      () => ({ render, ref }),
      [render, ref]
    );
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_root_context.RootContext.Provider, { value: rootContextValue, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
      import_react.DayPicker,
      {
        ...import_constants.COMMON_PROPS,
        ...localizationProps,
        ...props,
        role: "application",
        mode: "range",
        numberOfMonths: (0, import_misc.clampNumberOfMonths)(numberOfMonths),
        disabled,
        excludeDisabled,
        min,
        max,
        labels,
        selected: selected ?? void 0,
        onSelect: setSelected,
        onDayMouseEnter: (date) => setHoveredDate(date),
        onDayMouseLeave: () => setHoveredDate(void 0),
        modifiers,
        modifiersClassNames: import_constants.MODIFIER_CLASSNAMES
      }
    ) });
  }
);
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  RangeCalendar,
  usePreviewRange
});
//# sourceMappingURL=range-calendar.cjs.map
