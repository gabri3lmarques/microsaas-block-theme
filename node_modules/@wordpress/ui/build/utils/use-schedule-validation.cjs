"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// packages/ui/src/utils/use-schedule-validation.ts
var use_schedule_validation_exports = {};
__export(use_schedule_validation_exports, {
  useScheduleValidation: () => useScheduleValidation
});
module.exports = __toCommonJS(use_schedule_validation_exports);
var import_element = require("@wordpress/element");
function useScheduleValidation(validate) {
  const validateRef = (0, import_element.useRef)(validate);
  validateRef.current = validate;
  const timerRef = (0, import_element.useRef)(null);
  const unmountedRef = (0, import_element.useRef)(false);
  const scheduleValidation = (0, import_element.useCallback)(() => {
    if (unmountedRef.current) {
      return;
    }
    if (timerRef.current) {
      clearTimeout(timerRef.current);
    }
    timerRef.current = setTimeout(() => {
      validateRef.current();
      timerRef.current = null;
    }, 0);
  }, []);
  (0, import_element.useEffect)(() => {
    unmountedRef.current = false;
    return () => {
      unmountedRef.current = true;
      if (timerRef.current) {
        clearTimeout(timerRef.current);
      }
    };
  }, []);
  return scheduleValidation;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  useScheduleValidation
});
//# sourceMappingURL=use-schedule-validation.cjs.map
