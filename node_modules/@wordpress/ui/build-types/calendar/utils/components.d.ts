import type { CalendarDay, RootProps, ChevronProps } from '@daypicker/react';
import type { Modifiers } from '../types';
/**
 * Render a grid cell for a specific day in the calendar.
 *
 * Handles interaction and focus for the day.
 * @see https://daypicker.dev/guides/custom-components
 */
export declare function Day(props: {
    /** The day to render. */
    day: CalendarDay;
    /** The modifiers to apply to the day. */
    modifiers: Modifiers;
} & React.HTMLAttributes<HTMLDivElement>): import("react").JSX.Element;
/**
 * Render the root element of the calendar.
 *
 * Wired to `useRender` so that consumers can swap the underlying element
 * through the `render` prop, like every other `@wordpress/ui` component.
 * @see https://daypicker.dev/guides/custom-components
 */
export declare function Root({ rootRef, ...props }: RootProps): import("react").ReactElement<any, string | import("react").JSXElementConstructor<any>>;
/**
 * Render the chevron icon used in the navigation buttons.
 * @see https://daypicker.dev/guides/custom-components
 */
export declare function Chevron({ orientation, className }: ChevronProps): import("react").JSX.Element;
/**
 * Render a month navigation button.
 *
 * `@daypicker/react` marks the button as `aria-disabled` (rather than
 * `disabled`) when there is no month to navigate to, so that it stays
 * discoverable. `Button`'s `focusableWhenDisabled` produces the same DOM.
 * @see https://daypicker.dev/guides/custom-components
 */
export declare function NavButton({ className, 'aria-disabled': ariaDisabled, ...props }: React.ButtonHTMLAttributes<HTMLButtonElement>): import("react").JSX.Element;
//# sourceMappingURL=components.d.ts.map