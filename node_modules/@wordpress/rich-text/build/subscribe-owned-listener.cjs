"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// packages/rich-text/src/subscribe-owned-listener.js
var subscribe_owned_listener_exports = {};
__export(subscribe_owned_listener_exports, {
  subscribeOwnedListener: () => subscribeOwnedListener
});
module.exports = __toCommonJS(subscribe_owned_listener_exports);
var import_compose = require("@wordpress/compose");
var import_owns_selection = require("./owns-selection.cjs");
var import_lock_unlock = require("./lock-unlock.cjs");
var { subscribeDelegatedListener } = (0, import_lock_unlock.unlock)(import_compose.privateApis);
function subscribeOwnedListener(element, eventType, callback, capture = false) {
  return subscribeDelegatedListener(
    element.ownerDocument,
    eventType,
    (event) => {
      if (!element.contains(event.target) && !(0, import_owns_selection.ownsSelection)(element)) {
        return;
      }
      callback(event);
    },
    capture
  );
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  subscribeOwnedListener
});
//# sourceMappingURL=subscribe-owned-listener.cjs.map
