import { PopoverAnchorOptions } from "../popover/popover-anchor.js";
import { t as ComboboxStore } from "../combobox-store-FilGAecN.js";
import { Props } from "@ariakit/react-utils";
import { ElementType } from "react";
//#region src/combobox/combobox-anchor.d.ts
declare const TagName = "div";
type TagName = typeof TagName;
/**
 * Returns props to create a `ComboboxAnchor` component.
 * @see https://ariakit.com/components/combobox
 * @example
 * ```jsx
 * const store = useComboboxStore();
 * const props = useComboboxAnchor({ store });
 * <Role {...props}>Anchor</Role>
 * <Combobox store={store} />
 * <ComboboxPopover store={store}>Content</ComboboxPopover>
 * ```
 */
declare const useComboboxAnchor: import("@ariakit/react-utils").Hook<"div", ComboboxAnchorOptions<"div">>;
/**
 * Renders an element that acts as the anchor for the
 * [`ComboboxPopover`](https://ariakit.com/reference/combobox-popover)
 * component.
 * @see https://ariakit.com/components/combobox
 * @example
 * ```jsx {3}
 * <ComboboxProvider>
 *   <Combobox />
 *   <ComboboxAnchor>Anchor</ComboboxAnchor>
 *   <ComboboxPopover>Content</ComboboxPopover>
 * </ComboboxProvider>
 * ```
 */
declare const ComboboxAnchor: (props: ComboboxAnchorProps) => import("react").ReactElement<unknown, string | import("react").JSXElementConstructor<any>>;
interface ComboboxAnchorOptions<T extends ElementType = TagName> extends PopoverAnchorOptions<T> {
  /**
   * Object returned by the
   * [`useComboboxStore`](https://ariakit.com/reference/use-combobox-store)
   * hook. If not provided, the closest
   * [`ComboboxProvider`](https://ariakit.com/reference/combobox-provider)
   * component's context will be used.
   */
  store?: ComboboxStore;
}
type ComboboxAnchorProps<T extends ElementType = TagName> = Props<T, ComboboxAnchorOptions<T>>;
//#endregion
export { ComboboxAnchor, ComboboxAnchorOptions, ComboboxAnchorProps, useComboboxAnchor };
//# sourceMappingURL=combobox-anchor.d.ts.map