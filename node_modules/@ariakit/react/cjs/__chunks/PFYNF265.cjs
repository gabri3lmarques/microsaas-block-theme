"use strict";Object.defineProperty(exports, "__esModule", {value: true});










var _PIEOUUSWcjs = require('./PIEOUUSW.cjs');


var _DR6ZEVD6cjs = require('./DR6ZEVD6.cjs');



var _W5C4UCUZcjs = require('./W5C4UCUZ.cjs');



var _VASGTWPWcjs = require('./VASGTWPW.cjs');






















var _D3E2TUKRcjs = require('./D3E2TUKR.cjs');

// ../ariakit-react-components/dist/composite/composite-item.js
var _react = require('react');
var _jsxruntime = require('react/jsx-runtime');
var TagName = "button";
function isEditableElement(element) {
  if (_D3E2TUKRcjs.isTextbox.call(void 0, element)) return true;
  return element.tagName === "INPUT" && !_D3E2TUKRcjs.isButton.call(void 0, element);
}
function getNextPageOffset(scrollingElement, pageUp = false) {
  const height = scrollingElement.clientHeight;
  const { top } = scrollingElement.getBoundingClientRect();
  const pageSize = Math.max(height * 0.875, height - 40) * 1.5;
  const pageOffset = pageUp ? height - pageSize + top : pageSize + top;
  if (scrollingElement.tagName === "HTML") return pageOffset + scrollingElement.scrollTop;
  return pageOffset;
}
function getItemOffset(itemElement, pageUp = false) {
  const { top } = itemElement.getBoundingClientRect();
  if (pageUp) return top + itemElement.clientHeight;
  return top;
}
function findNextPageItemId(element, store, next, pageUp = false) {
  var _a;
  if (!store) return;
  if (!next) return;
  const { renderedItems } = store.getState();
  const scrollingElement = _D3E2TUKRcjs.getScrollingElement.call(void 0, element);
  if (!scrollingElement) return;
  const nextPageOffset = getNextPageOffset(scrollingElement, pageUp);
  let id;
  let prevDifference;
  for (let i = 0; i < renderedItems.length; i += 1) {
    const previousId = id;
    id = next(i);
    if (!id) break;
    if (id === previousId) continue;
    const itemElement = (_a = _PIEOUUSWcjs.getEnabledItem.call(void 0, store, id)) == null ? void 0 : _a.element;
    if (!itemElement) continue;
    const difference = getItemOffset(itemElement, pageUp) - nextPageOffset;
    const absDifference = Math.abs(difference);
    if (pageUp && difference <= 0 || !pageUp && difference >= 0) {
      if (prevDifference !== void 0 && prevDifference < absDifference) id = previousId;
      break;
    }
    prevDifference = absDifference;
  }
  return id;
}
function targetIsAnotherItem(event, store) {
  if (_D3E2TUKRcjs.isSelfTarget.call(void 0, event)) return false;
  return _PIEOUUSWcjs.isItem.call(void 0, store, event.target);
}
var useCompositeItem = _D3E2TUKRcjs.createHook.call(void 0, function useCompositeItem2({ store, rowId: rowIdProp, preventScrollOnKeyDown = false, moveOnKeyPress = true, tabbable = false, getItem: getItemProp, typeaheadText, "aria-setsize": ariaSetSizeProp, "aria-posinset": ariaPosInSetProp, unstable_scrollIntoView: scrollIntoView, ...props }) {
  const context = _PIEOUUSWcjs.useCompositeScopedContext.call(void 0, );
  store = store || context;
  const id = _D3E2TUKRcjs.useId.call(void 0, props.id);
  const ref = _react.useRef.call(void 0, null);
  const mountedElementRef = _react.useRef.call(void 0, null);
  const markUnmountingRef = _react.useCallback.call(void 0, (element) => {
    const mountedElement = mountedElementRef.current;
    if (!element && mountedElement) _PIEOUUSWcjs.markItemUnmounting.call(void 0, mountedElement);
    if (element) _PIEOUUSWcjs.markItemMounted.call(void 0, element);
    mountedElementRef.current = element;
  }, []);
  const row = _react.useContext.call(void 0, _PIEOUUSWcjs.CompositeRowContext);
  const trulyDisabled = _D3E2TUKRcjs.disabledFromProps.call(void 0, props) && !props.accessibleWhenDisabled;
  const shouldRegisterItem = props.shouldRegisterItem;
  const getRowId = (state) => {
    if (rowIdProp) return rowIdProp;
    if (!state) return;
    if (!(row == null ? void 0 : row.compositeElement)) return;
    if (row.compositeElement !== state.compositeElement) return;
    return row.id;
  };
  const { rowId, compositeElement, ariaSetSize, ariaPosInSet } = _W5C4UCUZcjs.useStoreStateObject.call(void 0, store, ["compositeElement", "renderedItems"], {
    rowId: getRowId,
    compositeElement(state) {
      return (state == null ? void 0 : state.compositeElement) || void 0;
    },
    ariaSetSize(state) {
      if (ariaSetSizeProp != null) return ariaSetSizeProp;
      if (!state) return;
      if (!(row == null ? void 0 : row.ariaSetSize)) return;
      if (row.compositeElement !== state.compositeElement) return;
      return row.ariaSetSize;
    },
    ariaPosInSet(state) {
      if (ariaPosInSetProp != null) return ariaPosInSetProp;
      if (!state) return;
      if (!(row == null ? void 0 : row.ariaPosInSet)) return;
      if (row.compositeElement !== state.compositeElement) return;
      const rowId2 = getRowId(state);
      const itemsInRow = state.renderedItems.filter((item) => item.rowId === rowId2);
      return row.ariaPosInSet + itemsInRow.findIndex((item) => item.id === id);
    }
  });
  const { isActiveItem, isTabbable } = _W5C4UCUZcjs.useStoreStateObject.call(void 0, store, [
    "activeId",
    "compositeElement",
    "renderedItems",
    "virtualFocus",
    "items"
  ], {
    isActiveItem(state) {
      return !!state && state.activeId === id;
    },
    isTabbable(state) {
      if (!state) return true;
      if (!state.compositeElement && !state.renderedItems.length) return true;
      if (state.virtualFocus) return false;
      if (!state.renderedItems.length) return true;
      if (tabbable) return true;
      if (state.activeId === null) return false;
      const item = store == null ? void 0 : store.item(state.activeId);
      if (item == null ? void 0 : item.disabled) return true;
      if (!(item == null ? void 0 : item.element)) return true;
      return state.activeId === id;
    }
  });
  const getItem = _react.useCallback.call(void 0, (item) => {
    var _a;
    const nextItem = {
      ...item,
      id: id || item.id,
      rowId,
      disabled: trulyDisabled,
      children: (_a = item.element) == null ? void 0 : _a.textContent,
      typeaheadText
    };
    if (getItemProp) return getItemProp(nextItem);
    return nextItem;
  }, [
    id,
    rowId,
    trulyDisabled,
    typeaheadText,
    getItemProp
  ]);
  const onFocusProp = props.onFocus;
  const hasFocusedComposite = _react.useRef.call(void 0, false);
  const cancelScheduledFocusRedirectRef = _react.useRef.call(void 0, null);
  const present = _PIEOUUSWcjs.usePresentItem.call(void 0, store);
  const onFocus = _D3E2TUKRcjs.useEvent.call(void 0, (event) => {
    onFocusProp == null ? void 0 : onFocusProp(event);
    if (event.defaultPrevented) return;
    if (_D3E2TUKRcjs.isPortalEvent.call(void 0, event)) return;
    if (!id) return;
    if (!store) return;
    if (targetIsAnotherItem(event, store)) return;
    const { virtualFocus, compositeElement: compositeElement2 } = store.getState();
    store.setActiveId(id);
    if (_D3E2TUKRcjs.isTextbox.call(void 0, event.currentTarget)) _PIEOUUSWcjs.selectTextField.call(void 0, event.currentTarget);
    if (!virtualFocus) {
      if (_D3E2TUKRcjs.isSelfTarget.call(void 0, event) && store.item(id)) present({
        id,
        markedOnly: true,
        requireFocus: true,
        scrollIntoView
      });
      return;
    }
    if (!_D3E2TUKRcjs.isSelfTarget.call(void 0, event)) return;
    if (isEditableElement(event.currentTarget)) return;
    const redirectFocusToCompositeElement = (relatedTarget2, compositeElement3) => {
      if (!_D3E2TUKRcjs.isFocusable.call(void 0, compositeElement3)) {
        if (process.env.NODE_ENV !== "production") _D3E2TUKRcjs.warnOnce.call(void 0, "A composite widget with `virtualFocus` enabled requires a focusable composite element. Set the `focusable` prop to `true` or the `virtualFocus` option to `false`.", compositeElement3);
        return;
      }
      const fromComposite = relatedTarget2 === compositeElement3 || _PIEOUUSWcjs.isItem.call(void 0, store, relatedTarget2);
      if (store.item(id)) present({
        id,
        markedOnly: true,
        requireFocus: true,
        scrollIntoView
      });
      hasFocusedComposite.current = true;
      if (fromComposite) _PIEOUUSWcjs.focusSilently.call(void 0, compositeElement3);
      else compositeElement3.focus({ preventScroll: true });
    };
    if (compositeElement2 == null ? void 0 : compositeElement2.isConnected) {
      redirectFocusToCompositeElement(event.relatedTarget, compositeElement2);
      return;
    }
    if (shouldRegisterItem === false) return;
    const { currentTarget, relatedTarget } = event;
    const cancelScheduledFocusRedirect = () => {
      var _a;
      (_a = cancelScheduledFocusRedirectRef.current) == null ? void 0 : _a.call(cancelScheduledFocusRedirectRef);
      cancelScheduledFocusRedirectRef.current = null;
    };
    cancelScheduledFocusRedirect();
    cancelScheduledFocusRedirectRef.current = _W5C4UCUZcjs.subscribe.call(void 0, store, null, () => {
      if (_D3E2TUKRcjs.getActiveElement.call(void 0, currentTarget) !== currentTarget) {
        cancelScheduledFocusRedirect();
        return;
      }
      const state = store.getState();
      const nextCompositeElement = state.compositeElement;
      if (!(nextCompositeElement == null ? void 0 : nextCompositeElement.isConnected)) return;
      cancelScheduledFocusRedirect();
      if (!state.virtualFocus) return;
      redirectFocusToCompositeElement(relatedTarget, nextCompositeElement);
    });
  });
  const onBlurCaptureProp = props.onBlurCapture;
  const onBlurCapture = _D3E2TUKRcjs.useEvent.call(void 0, (event) => {
    var _a;
    onBlurCaptureProp == null ? void 0 : onBlurCaptureProp(event);
    if (event.defaultPrevented) return;
    if (((_a = store == null ? void 0 : store.getState()) == null ? void 0 : _a.virtualFocus) && hasFocusedComposite.current) {
      hasFocusedComposite.current = false;
      event.preventDefault();
      event.stopPropagation();
    }
  });
  const onKeyDownProp = props.onKeyDown;
  const preventScrollOnKeyDownProp = _D3E2TUKRcjs.useBooleanEvent.call(void 0, preventScrollOnKeyDown);
  const moveOnKeyPressProp = _D3E2TUKRcjs.useBooleanEvent.call(void 0, moveOnKeyPress);
  const onKeyDown = _D3E2TUKRcjs.useEvent.call(void 0, (event) => {
    var _a;
    onKeyDownProp == null ? void 0 : onKeyDownProp(event);
    if (event.defaultPrevented) return;
    if (!_D3E2TUKRcjs.isSelfTarget.call(void 0, event)) return;
    if (!store) return;
    const { currentTarget } = event;
    const state = store.getState();
    const isGrid = !!((_a = store.item(id)) == null ? void 0 : _a.rowId);
    const isVertical = state.orientation !== "horizontal";
    const isHorizontal = state.orientation !== "vertical";
    const canHomeEnd = () => {
      if (isGrid) return true;
      if (isHorizontal) return true;
      if (!state.compositeElement) return true;
      if (!_D3E2TUKRcjs.isTextField.call(void 0, state.compositeElement)) return true;
      return false;
    };
    const action = {
      ArrowUp: (isGrid || isVertical) && store.up,
      ArrowRight: (isGrid || isHorizontal) && store.next,
      ArrowDown: (isGrid || isVertical) && store.down,
      ArrowLeft: (isGrid || isHorizontal) && store.previous,
      Home: () => {
        if (!canHomeEnd()) return;
        if (!isGrid || event.ctrlKey) return store == null ? void 0 : store.first();
        return store == null ? void 0 : store.previous(-1);
      },
      End: () => {
        if (!canHomeEnd()) return;
        if (!isGrid || event.ctrlKey) return store == null ? void 0 : store.last();
        return store == null ? void 0 : store.next(-1);
      },
      PageUp: () => {
        return findNextPageItemId(currentTarget, store, store == null ? void 0 : store.up, true);
      },
      PageDown: () => {
        return findNextPageItemId(currentTarget, store, store == null ? void 0 : store.down);
      }
    }[event.key];
    if (action) {
      if (_D3E2TUKRcjs.isTextbox.call(void 0, currentTarget)) {
        const selection = _D3E2TUKRcjs.getTextboxSelection.call(void 0, currentTarget);
        const isLeft = isHorizontal && event.key === "ArrowLeft";
        const isRight = isHorizontal && event.key === "ArrowRight";
        const isUp = isVertical && event.key === "ArrowUp";
        const isDown = isVertical && event.key === "ArrowDown";
        if (isRight || isDown) {
          const { length: valueLength } = _D3E2TUKRcjs.getTextboxValue.call(void 0, currentTarget);
          if (selection.end !== valueLength) return;
        } else if ((isLeft || isUp) && selection.start !== 0) return;
      }
      const nextId = action();
      if (preventScrollOnKeyDownProp(event) || nextId !== void 0) {
        if (!moveOnKeyPressProp(event)) return;
        event.preventDefault();
        store.move(nextId);
      }
    }
  });
  const providerValue = _react.useMemo.call(void 0, () => ({
    id,
    compositeElement
  }), [id, compositeElement]);
  props = _D3E2TUKRcjs.useWrapElement.call(void 0, props, (element) => /* @__PURE__ */ _jsxruntime.jsx.call(void 0, _PIEOUUSWcjs.CompositeItemContext.Provider, {
    value: providerValue,
    children: element
  }), [providerValue]);
  props = {
    "data-active-item": isActiveItem || void 0,
    ...props,
    id,
    ref: _D3E2TUKRcjs.useMergeRefs.call(void 0, ref, markUnmountingRef, props.ref),
    tabIndex: isTabbable ? props.tabIndex : -1,
    onFocus,
    onBlurCapture,
    onKeyDown
  };
  props = _VASGTWPWcjs.useCommand.call(void 0, props);
  props = _DR6ZEVD6cjs.useCollectionItem.call(void 0, {
    store,
    ...props,
    getItem,
    shouldRegisterItem: id ? shouldRegisterItem : false
  });
  return {
    ...props,
    "aria-setsize": ariaSetSize,
    "aria-posinset": ariaPosInSet
  };
});
var CompositeItem = _D3E2TUKRcjs.memo.call(void 0, _D3E2TUKRcjs.forwardRef.call(void 0, function CompositeItem2(props) {
  const htmlProps = useCompositeItem(_VASGTWPWcjs.withDefaultButtonType.call(void 0, props));
  return _D3E2TUKRcjs.createElement.call(void 0, TagName, htmlProps);
}));




exports.useCompositeItem = useCompositeItem; exports.CompositeItem = CompositeItem;
